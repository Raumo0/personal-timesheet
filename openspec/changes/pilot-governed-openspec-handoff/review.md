## Independent review plan

### Scope and inputs

- Inspect the native apply context, implementation-loop execution state, and
  fresh canonical validation evidence for pilot task 1.1.

### Verdict

- Record `APPROVED` or `NEEDS_FIXES` in the receipt-bound reviewer report.
- Mark the pilot checkbox only after independent `APPROVED`.

### Human Gate

- 2026-08-02: the user approved the exact `openspec/config.yaml` default-schema
  change from `spec-driven` to `governed-spec-driven` after reviewing the
  successful no-wrapper pilot outcome.
